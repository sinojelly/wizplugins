使用方法

修改config.ini，把git.exe路径和本地git库工作目录路径配置正确。如果本地没有git库工作目录，可以用git init新建一个。

可以用github、google code(git) 来作为远端服务器，把本地git库所有信息备份过去。

我主要在用git，hg应该也能支持。svn自己写个批处理应该也可以，但不太推荐，它虽然能保存历史，但很多方面都不及分布式版本管理工具。


使用git，需要先在本机安装git，Windows上可以安装cygwin+git，也可以安装 Mingw+git，前者可能功能更全。
